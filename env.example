# Santa Monica Activities Browser
# Copy this file to .env and fill in your values.

# Base URL for the ActiveNet API — no trailing slash, no changes needed.
BASE_URL=https://anc.apm.activecommunities.com/santamonicarecreation/rest

# X-CSRF-Token header value.
# It looks like a UUID: e.g. 7d5fc3f0-f188-4c32-8b6b-aa20d6487ae8
#
# How to get it:
# 1. Log in at https://anc.apm.activecommunities.com/santamonicarecreation/
# 2. Open DevTools (F12) -> Network tab
# 3. Click "Activities" to trigger a search
# 4. Find the POST request to /rest/activities/list
# 5. Under "Request Headers", copy the X-CSRF-Token value
CSRF_TOKEN=

# Full Cookie header value from the same request.
# IMPORTANT: Wrap the value in double quotes — it contains semicolons and equals
# signs that would otherwise confuse the .env parser.
#
# How to get it:
# 1. In the same POST request from above
# 2. Under "Request Headers", find the Cookie header
# 3. Copy the entire value (long string of key=value pairs separated by "; ")
# 4. Paste it inside double quotes as shown:
#
# SESSION_COOKIE="JSESSIONID=node0abc...; santamonicarecreation_JSESSIONID=node0xyz...; ..."
#
# Note: Sessions expire after a period of inactivity (typically a few hours).
# When the app stops returning results, repeat the steps above to refresh these values.
SESSION_COOKIE=""
